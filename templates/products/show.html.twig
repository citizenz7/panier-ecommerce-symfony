{% extends 'base.html.twig' %}

{% block title %}Produit : {{ product.title }}{% endblock %}

{% block body %}
    <h1>{{ product.title }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ product.title }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ product.image }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ product.price }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ product.description }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-sm btn-primary" href="{{ path('products_index') }}">Retour à la liste</a>

    <a class="btn btn-sm btn-warning" href="{{ path('products_edit', {'id': product.id}) }}">modifier</a>

    {{ include('products/_delete_form.html.twig') }}
{% endblock %}
